<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yangchenle.electricityconsumptionsystem.dao.UserDao">
    <resultMap id="User" type="com.yangchenle.electricityconsumptionsystem.entity.UserEntity">
        <result column="UserId" property="userId"/>
        <result column="UserName" property="userName"/>
        <result column="UserPhone" property="userPhone"/>
        <result column="UserAccount" property="userAccount"/>
        <result column="UserAddress" property="userAddress"/>
        <result column="IdCard" property="idCard"/>
        <result column="AddTime" property="addTime"/>
        <result column="UpdateTime" property="updateTime"/>
    </resultMap>

    <sql id="allFile">
        UserId,
        UserName,
        UserPhone,
        UserAccount,
        UserAddress,
        IdCard,
        AddTime,
        UpdateTime
    </sql>

    <select id="userLogin" parameterType="map" resultMap="User">
        SELECT
        <include refid="allFile"/>
        FROM user_account
        WHERE
        UserPhone = #{userPhone}
    </select>

    <select id="insertUser" parameterType="map">
        INSERT INTO
        user_account(UserId, UserName, UserPhone, UserAccount, UserAddress, AddTime, UpdateTime, IdCard)
        VALUES (
        #{userId },
        #{userName },
        #{userPhone },
        #{userAccount },
        #{userAddress },
        NOW(),
        NOW(),
        #{idCard }
        )
    </select>
</mapper>